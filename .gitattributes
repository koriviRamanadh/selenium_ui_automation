# Auto-detect text files and normalize line endings
* text=auto

# Shell scripts should always use LF
*.sh text eol=lf

# PowerShell scripts should use CRLF
*.ps1 text eol=crlf

# Batch files (Windows)
*.bat text eol=crlf

# Java source files (normalize to LF in repo)
*.java text eol=lf
